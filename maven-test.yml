---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: 3.6.0-jdk-8-alpine

inputs:
  - name: spring-boot-service       # konse input resource pe chlani h command

run:
  path: mvn
  args:
    - clean
    - test
